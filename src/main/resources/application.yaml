spring:
  application:
    name: wedding-app

  datasource:
    url: jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres
    username: postgres.wzrfwuycstibknqrbayb
    password: Swapnil@1505
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update        # auto-creates/updates tables from your entities
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: suppykumar@gmail.com
    password: Swapnil@123   # Gmail App Password, not your login password
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

server:
  port: 8080

jwt:
  secret: your-256-bit-secret-key-here-make-it-long-and-random
  expiration: 86400000    # 24 hours in ms

app:
  frontend-url: http://localhost:5173   # React dev server